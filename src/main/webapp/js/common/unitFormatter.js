function ErlFormatter(value, row, index) {
	value = value.toFixed(2);
	value = value + "Erl";
	return value;
}
function GBFormatter(value, row, index) {
	value = value.toFixed(2);
	value = value + "GB";
	return value;
}
